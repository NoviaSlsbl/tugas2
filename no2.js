function maxmin(array) {
    const no = array.filter(no=>{
        return no < 3
    })
    console.log(no);
}
maxmin([1, 2, 3]);


function maxmin(array) {
    const no = array.filter(no=>{
        return no < 2
    })
    console.log(no);
}
maxmin([1, 2, 3]);

function maxmin(array) {
    const no = array.filter(no=>{
        return no < 1
    })
    console.log(no);
}
maxmin([1, 2, 3]);

function maxmin(array) {
    const no = array.filter(no=>{
        return no = 3
    })
    console.log(no);
}
maxmin([1, 2, 3],0);


